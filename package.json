{
  "name": "v8-mod-tutor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "cf:build": "bun run build && wrangler deploy --dry-run",
    "cf:deploy": "bun run build && wrangler deploy",
    "preview": "vite preview",
    "start": "bun run build && bun run preview"
  },
  "prettier": {
    "semi": false
  },
  "dependencies": {
    "highlight.js": "^11.12.0",
    "lucide": "^1.31.0",
    "vite-plugin-static-md": "^0.5.5"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^24.3.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vite": "^7.0.0",
    "wrangler": "4.123.0"
  }
}
